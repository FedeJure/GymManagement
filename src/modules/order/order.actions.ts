import { Dispatch } from "redux"
import { OrderPayload } from "./OrderPayload"

export const getOrdersAction = ({ page, step, filterByContent, append }: { page: number, step?: number, filterByContent?: string[], append: boolean }) => {
    return (dispatch: Dispatch) => {

    }
}

export const createOrderAction = (data: OrderPayload) => {
    return (dispatch: Dispatch) => {

    }
}